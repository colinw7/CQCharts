load -csv pareto.csv -comment_header

@let model = rc

process -add -header Sum "cell(@r,1)+cell(@r-1,@c,0)"
process -add -header Percent "100.0*column(1)/cell(5,2)"

model -ind "${model}" -column_type "3#real:format=%g%%"

plot -type bar -columns "name=0,value=1" -ymax 150000

@let plot1 = rc

view -view view1 -title "Popular Fast Food Chains"

set -view view1 -plot "${plot1}" -name "Y Axis.grid.line.visible" -value 1

plot -type xy -columns "x=0,y=3" -ymin 0

@let plot2 = rc

set -view view1 -plot "${plot2}" -name cumulative.enabled  -value 1
set -view view1 -plot "${plot2}" -name points.symbol       -value circle
set -view view1 -plot "${plot2}" -name points.size         -value 6
set -view view1 -plot "${plot2}" -name points.fill.visible -value 1
set -view view1 -plot "${plot2}" -name lines.width         -value 3

y1y2 "${plot1}" "${plot2}"
