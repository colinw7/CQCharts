. each column value is bar height

Value

------------+--------------------+---------------------+
            | single column      | multiple columns    |
------------+--------------------+---------------------+
no group    | bar per value      | bar per value       |
            | color per value    | color per column    |
            | key item per value | key item per column |
------------+--------------------+---------------------+
group       | bar per value      | bar per value       |
(by column) | color per group    | color by column     |
            | key item per group | key item per column |
------------+--------------------+---------------------+
group       | bar per value      | bar per value       |
(by group)  | color per group    | color by group      |
            | key item per group | key item per group  |
------------+--------------------+---------------------+

Range, Min, Max, Mean. Sum (Grouped)

---------+---------------------+---------------------+
         | single column       | multiple columns    |
---------+---------------------+---------------------+
no group | single bar (column) | bar per column      |
         | single color        | color per column    |
         | key item per column | key item per column |
---------+---------------------+---------------------+
group    | bar per group       | bar per column      |
         | color per group     | color per column    |
         | key item per group  | key item per column |
---------+---------------------+---------------------+

Color By Set
